// 문자열안에 문자열

/**
 * str1안에 str2가 포함된다면 1을, 없다면 2를 return
 */

function solution(str1, str2) {
  return str1.includes(str2) ? 1 : 2;
}

// https://school.programmers.co.kr/learn/courses/30/lessons/120908
